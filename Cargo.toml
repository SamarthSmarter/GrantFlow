[workspace]
resolver = "2"
members = [
    "contracts/grant_registry",
    "contracts/milestone_escrow",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
