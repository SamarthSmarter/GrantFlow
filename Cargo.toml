[workspace]
resolver = "2"
members = [
    "contracts/grant_registry",
    "contracts/milestone_escrow",
    "contracts/dispute_resolver",
    "contracts/invoice_registry",
    "contracts/payment_manager",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
